{
    "title"      : "Status",
    "type"       : "object",
    "required"   : [ "values", "groups" ],
    "properties" : {
        "values" : {
            "type" : "object",
            "patternProperties" : {
                "^[A-Z][A-Za-z]*$" : {
                    "type" : "number"
                }
            }
        },
        "groups" : {
            "type" : "object",
            "patternProperties" : {
                "^[A-Z][A-Za-z]*$" : {
                    "type"  : "array",
                    "items" : { "type" : "string" }
                }
            }
        }
    }
}
