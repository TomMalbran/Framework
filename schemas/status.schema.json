{
    "title" : "Status",
    "type"  : "object",
    "propertyNames" : {
        "pattern" : "^[A-Z][A-Za-z]*$"
    },
    "patternProperties" : {
        "^[A-Z][A-Za-z]*$" : {
            "type"  : "array",
            "items" : {
                "type"    : "string",
                "pattern" : "^[A-Z][A-Za-z]*$"
            }
        }
    }
}
